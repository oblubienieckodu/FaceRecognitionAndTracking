package com.uploader.Service;

import com.uploader.Entity.FileEntity;

import java.io.File;
import java.util.ArrayList;
import java.util.List;

public class FileService {
/*    public List<FileEntity> getFilesFromDirectory(String path){
        File folder = new File(UPLOADED_FOLDER);
        File[] listOfFiles = folder.listFiles();
        ArrayList<FileEntity> listOfFilename = new ArrayList<>();

        for (int i = 0; i < listOfFiles.length; i++) {
            if (listOfFiles[i].isFile()) {
                listOfFilename.add(listOfFiles[i].getName());
            }
        }
    }*/
}
